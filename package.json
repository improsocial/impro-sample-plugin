{
  "name": "impro-sample-plugin",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm run watch",
    "build": "esbuild src/main.js --bundle --format=esm --outfile=main.js",
    "watch": "esbuild src/main.js --bundle --format=esm --outfile=main.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
